### Deprecated

- `--interop-num-validators`, `--interop-start-index`, `--interop-eth1data-votes` and `--interop-write-ssz-state-transitions` are now hidden no-ops. Use [ethereum-package](https://github.com/ethpandaops/ethereum-package) for interop/devnet testing.

### Removed

- Removed the interop keymanager, mocked eth1 data votes, the SSZ state-transition dump helpers (`tools/extractor`), and the `interop` chain-config preset.
