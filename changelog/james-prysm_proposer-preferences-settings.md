### Changed

- proposer settings no longer recognize the builder option post gloas and introduces a new gas_limit option in the default for proposer preferences
- gas limit keymanager endpoint will update global gas limit default instead of a per validator basis post gloas.
