### Fixed

- Clarified `validator accounts exit` prompts and final log message when `--exit-json-output-dir` is set: signed exits are only written to files and are not broadcast, so the messaging no longer implies validators were exited.
